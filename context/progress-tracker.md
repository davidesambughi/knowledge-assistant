# Progress Tracker

<!-- The AI updates this file after every meaningful implementation change.
     This is how work resumes across sessions without losing context.
     Never delete completed work — it's the audit trail di decisioni prese. -->

---

## Current Phase

Phase 3 — `03a`-`03i` completi e verificati end-to-end (build, lint, test, dev server reale) — pronti per `05a — Deploy su Vercel` (feature-list rinumerata, vedi §Next Up)

---

## Current Goal

git init + repo GitHub + push, poi `05a — Deploy su Vercel` (`feature-list.md`, rinumerato da `04a` dopo l'inserimento di `04 — Project Overview Panel`).

---

## Completed

- **00 — Project Setup & Environment** — Next.js 16.3.1 + TS `~6.0.3`, shadcn/ui (Base UI, `base-nova`), Supabase (`document_chunks` + indice HNSW + RLS senza policy), `.env.local`/`lib/env.ts` validati, build ok. Spec: `feature-spec/00-project-setup.md`.
- **01a — Markdown Parsing & Chunking** — `lib/rag/chunking.ts` (`chunkMarkdownFile`, fence-aware — protegge qualunque blocco fenced, non solo Mermaid) + `lib/rag/ingest.ts` (`readCorpus`, non ricorsivo). Verificato su corpus reale (8 file, 200 chunk): heading-path corretti, nessuno split spurio dentro blocchi bash con `#`. `npm run ingest:preview` (tsx). 7 test. Spec: `feature-spec/01a-markdown-chunking.md`.
- **01b — Embeddings & Storage** — `lib/supabase/admin.ts`, `EmbeddingResultSchema`, `lib/rag/embeddings.ts` (`@google/genai`, `gemini-embedding-001`, batch 20, 1536 dim via `outputDimensionality`), `buildDocumentRows`/`runIngest`. 17 test. Percorso non lineare: OpenAI scartato per mancanza di free tier reale (429 quota) → Gemini → `403 SERVICE_DISABLED` → API abilitata → `403 PERMISSION_DENIED` transitorio → terzo run riuscito senza modifiche di codice (propagazione lato Google). Verificato: 200 righe scritte, re-run idempotente (200 invariate), embedding a 1536 dim confermati su Supabase. Spec: `feature-spec/01b-embeddings-storage.md`.
- **02 — Retrieval (naive + hybrid)** — `RetrievedChunkSchema`, `embedQuery` (`taskType: RETRIEVAL_QUERY`, ancora supportato da `gemini-embedding-001`), `lib/rag/retrieval.ts` (`TOP_K`, `mapRowToRetrievedChunk`, `retrieveChunks`, `hybridRetrieveChunks`). SQL manuale su Supabase: `fts`/GIN, `match_documents` (cosine), `hybrid_search` (RRF lato SQL). Verificato: naive ritorna 5 chunk pertinenti su query reale; hybrid recupera un chunk ("Idempotency") che il solo vettoriale non trova nei primi 100 — upgrade confermato. 2 test, 19/19 totali. **Bug fixato**: `LIMIT` senza `ORDER BY` esplicito in `hybrid_search` (vedi §Architecture Decisions). Spec: `feature-spec/02-retrieval.md`.
- **03a — Chat API (backend)** — `ChatRequestSchema`, `buildSystemPrompt` (Invariant #11/#12/#19 + istruzione bilingue), `lib/rag/generation.ts` (`streamChatResponse`, provider Google), `app/api/chat/route.ts` (valida, retrieval, streaming via text-stream protocol). 5 test, 24/24 totali. Verificato via curl: domanda in-corpus → risposta con citazioni; fuori corpus → rifiuto (#11); prompt injection → nessun bypass (#19); body malformato → 400. **Deviazione**: `gemini-2.5-flash` (da spec) rifiutato a runtime ("no longer available to new users") → sostituito `gemini-3.1-flash-lite`, verificato via `GET /v1beta/models`. Spec: `feature-spec/03a-chat-api.md`.
- **03b — Chat UI** — `app/page.tsx` (`useChat` + `TextStreamChatTransport`), `lib/chat/messages.ts` (`toChatRequestMessages`/`isEmptyAssistantResponse`, 9 test), `next-intl` senza i18n routing (cookie `NEXT_LOCALE`), `locale-switch.tsx`, Markdown rendering, font Inter, tema Blue. Verificato in browser: streaming + Markdown ok, citazioni multiple fonte, rifiuto fuori-corpus confermato anche in UI, multi-turno ok, switch lingua ok, refresh azzera conversazione (voluto). 33/33 test. Spec: `feature-spec/03b-chat-ui.md`.
- **03c — Rate Limiting** — `lib/rate-limit.ts` (`@upstash/ratelimit`, sliding window 10 req/10 min, `getClientIp` via `ipAddress()` da `@vercel/functions`), integrato come primo step di `POST` prima del parsing body — `429` + `Retry-After` oltre soglia. 2 test, 35/35 totali. Verificato: 10 req → 200, 11ª → 429, IP diverso → isolato/200. **Deviazione dalla spec**: `ipAddress()` legge solo `x-real-ip`, non `x-forwarded-for` come assunto — verificato nel sorgente del pacchetto, spec e test corretti. Spec: `feature-spec/03c-rate-limiting.md`.
- **03d — Security Review** — Ricerca dedicata (OWASP GenAI/LLM Top 10 2026). Hardening `/api/chat`: `MAX_MESSAGE_LENGTH`/`MAX_MESSAGES_PER_REQUEST`/`MAX_TOTAL_REQUEST_LENGTH` (terzo limite chiude il worst-case combinato dei primi due, 160k caratteri altrimenti), `GENERATION_MAX_OUTPUT_TOKENS`, errori di retrieval ora generici al client (dettaglio solo in log), `lib/security.ts::isAllowedOrigin` (auto-referenziale, non allowlist hardcoded — assume deploy diretto su Vercel), prompt rinforzato contro leak del system prompt. Invariants #18-20 riverificati. 9 nuovi test, 44/44 totali. Verificato end-to-end: Origin estraneo → 403, limiti di lunghezza → 400, retrieval failure → messaggio generico al client + stack in log, rate limit → 429 dopo 10 richieste. **3 correzioni post-review esterna, tutte verificate prima di accettarle**: (1) `.min(1)` mancante su `messages` (non era una regressione né un crash reale — guard a valle già presente — ma il vincolo andava dichiarato al confine di validazione); (2) `isAllowedOrigin` non copriva `Origin: "null"`/valore malformato — comportamento già sicuro (403), aggiunti test espliciti; (3) commento stantio in `generation.ts` corretto, `isOversizedRejection` passato da match su `code: "custom"` generico a un tag Zod dedicato (`superRefine` + `params.tag`) per non confondersi con futuri `.refine()`. 46/46 test finali. Spec: `feature-spec/03d-security-review.md`.
- **03e — UI/UX Polish (Technical Knowledge Dashboard)** — Dashboard tecnica invece di generico chat widget: `source_file`/`heading_path` in `font-mono`, componenti in `components/chat/`, empty state con chip di prova (incluso un caso fuori-corpus per #11), callout di rifiuto in tono neutro (non destructive/rosso — il rifiuto è comportamento corretto). Tipografia: Inter (sans) + Geist Mono (mono). 3 nuovi test, 49/49 totali. Spec: `feature-spec/03e-ui-ux-polish.md`.
- **03f — Open Questions Cleanup (pre-04a)** — (1) `EMBEDDING_TIMEOUT_MS` cablata via `abortSignal: AbortSignal.timeout(...)` in `embedTexts`/`embedQuery` — `config.httpOptions.timeout` verificato rotto in `googleapis/js-genai` (issue #1277/#712, non fixate sulla major 2.x installata). Verificato abbassando a 1ms (fail immediato) e ripristinando a 30s (ingest ok, idempotenza invariata). (2) Retrieval cross-linguale chiuso come non-problema, vedi §Open Questions. 49/49 test (invariati). Spec: `feature-spec/03f-open-questions-cleanup.md`.
- **03g — System Prompt Language Anchoring Fix** — Bug: domanda EN → risposta IT a meno di richiesta esplicita ("answer in English"). Causa: `BASE_INSTRUCTIONS` in `lib/rag/prompt.ts` era prosa italiana continua, con la regola sulla lingua come ultima frase — language anchoring verso l'italiano (segnale debole) perdeva contro la lingua dominante del system prompt, vinto solo da una richiesta esplicita nel messaggio utente (segnale forte). Fix: system prompt riscritto in inglese con struttura a tag XML (`<role>`, `<constraints>`, `<context>`), regola lingua messa per prima in `<constraints>` invece che per ultima — pattern raccomandato esplicitamente da `ai.google.dev/gemini-api/docs/prompting-strategies` per Gemini 3 (verificato: doc aggiornata 10 giugno 2026, applicabile perché il progetto usa `gemini-3.1-flash-lite`). Secondo problema chiuso nella stessa unit: `prompt.test.ts` verificava solo la _presenza_ della stringa istruzione-lingua, mai la sua posizione — aggiunto test dedicato che verifica che l'istruzione preceda il blocco `<context>`. next-intl escluso come causa: il locale UI (cookie `NEXT_LOCALE`) non viene mai passato a `buildSystemPrompt`/`streamChatResponse`, catena verificata. 50/50 test. Verificato via dev server reale (`/api/chat`, non solo unit test): domanda EN → risposta EN, domanda IT → risposta IT, richiesta esplicita "answer in English" → invariata; Invariant #11/#19 e anti-leak riverificati col nuovo prompt. Spec: `feature-spec/03g-system-prompt-language-fix.md`.
- **03i — Gestione Errore Quota Gemini** — `lib/rag/generation.ts`: `isQuotaExceededError` (riconosce `APICallError` con `statusCode 429` o `data.error.status === "RESOURCE_EXHAUSTED"`), `resolveChatStream` (sbircia il primo elemento di `result.fullStream` prima di avviare la risposta al client — necessario perché `streamText()` non lancia mai in modo sincrono per un errore Gemini, emerge solo come part `"error"` dentro lo stream). `app/api/chat/route.ts`: try/catch aggiunto attorno a `streamChatResponse` (gap reale, throw sincrono non gestito) + branch su `resolveChatStream` → `503` con messaggio pulito invece di stream vuoto/500. `lib/chat/ui-helpers.ts`: `extractServerErrorMessage` (parsa il body JSON grezzo che `useChat`/`TextStreamChatTransport` propaga come `Error.message` — verificato in `node_modules/ai`, non assunto). `app/page.tsx`: il banner d'errore ora mostra il messaggio reale del server quando riconoscibile, non solo il generico `t("streamError")` (bug pre-esistente scoperto in fase di spec: il messaggio server non veniva mai letto). **Attraversa deliberatamente il boundary server/UI (`03a` + `03b`/`03e`) nello stesso step**: un fix solo lato server sarebbe rimasto invisibile finché la UI non mostra davvero il messaggio — dividere avrebbe lasciato il primo step non verificabile end-to-end. 13 nuovi test (`generation.test.ts`, `ui-helpers.test.ts`), 62/62 totali. Verificato: build/lint puliti, richiesta normale via curl invariata (nessun chunk perso dal meccanismo di peek), ramo quota verificato con `APICallError`/stream fabbricati (non una vera quota Gemini esaurita, come da §Testing della spec). **2 correzioni post-review esterna sulla spec (prima dell'implementazione), entrambe verificate leggendo i sorgenti reali**: (1) l'assunzione su `app/page.tsx` era infondata come critica — era già stata letta dal file reale, non dedotta da `03b`; (2) la forma di `error.message` di `useChat` su risposta non-2xx non era ancora verificata a sorgente — confermata in `HttpChatTransport.sendMessages` (`node_modules/ai`). Spec: `feature-spec/03i-quota-error-handling.md`.
- **03h — Rimozione LocaleSwitch (UI fissa in inglese)** — Lo switch IT/EN nell'header (`components/locale-switch.tsx`) cambiava solo le stringhe statiche della UI, mai la lingua della risposta del modello (quella segue la domanda, `03g`) — giudicato fuorviante/inutile, rimosso senza mini-spec dedicata (task piccolo, isolato alla UI). Modifiche: `components/locale-switch.tsx` eliminato; `chat-header.tsx` non lo importa/renderizza più; `lib/i18n/locale.ts` → `DEFAULT_LOCALE="en"`, `SUPPORTED_LOCALES=["en"]`; `messages/it.json` eliminato, chiave `LocaleSwitch` (già inutilizzata) rimossa da `messages/en.json`. Infrastruttura `next-intl`/`i18n/request.ts` mantenuta invariata (fallback su `DEFAULT_LOCALE`, ora sempre `"en"`) per un eventuale multi-locale futuro. **Deviazione dichiarata** da `project-overview.md` riga 35/§Aggiornamento scope — bilingue: "UI bilingue IT/EN" → "UI fissa in inglese"; la parte di scope "risposta del modello nella lingua della domanda" resta invariata e non è toccata da questa unit. 50/50 test invariati (nessun test copriva `LocaleSwitch`). Verificato: build ok, homepage renderizza (200, "Knowledge Assistant" presente), nessun riferimento residuo nel codice.

---

## In Progress

- None yet.

---

## Next Up

<!-- Note grezze da conversazione con un altro LLM (2026-08-23), non ancora rifinite — vedi
     `feature-list.md` per dove sono state agganciate e §Open Questions sotto per i dettagli
     non ancora decisi. Ordine relativo tra questi item non ancora deciso, tranne dove indicato. -->

- `04` — Project Overview Panel (recruiter-facing) — idea grezza, da rifinire in una feature-spec dedicata prima dell'implementazione (vedi `feature-list.md`). Confermato 2026-08-23: solo questo pannello sarà bilingue IT/EN (switch dedicato), il resto della UI resta fisso in inglese come da `03h` — non è un ripristino di `LocaleSwitch`.
- git init + repo GitHub + push — repo locale già inizializzato (commit iniziale presente), nessun remote configurato. Da fare prima di collegare Vercel in `05a`.
- `05a` — Deploy su Vercel, collegato al repo GitHub (non deploy manuale via CLI) così ogni push futuro rideploya in automatico. Env vars su dashboard, verifica Invariant #20 in produzione, smoke test, security header a livello app — vedi nota sotto.
- Sottodominio dedicato: dominio esistente `davidesambughi.dev` (Porkbun, già in uso per il portfolio live su un progetto Vercel separato — nessuna condivisione). Nuovo record CNAME su Porkbun verso il progetto Vercel del Knowledge Assistant. Nome esatto non ancora deciso (es. `kb.davidesambughi.dev`).
- `05b` — Collegamento portfolio (link dal project-detail di Remote NIF).

---

## Open Questions

- ~~Risposta Markdown grezza in UI~~ — RISOLTA: `react-markdown` aggiunto per messaggi assistant.
- ~~Retrieval cross-linguale mai riverificato dopo Gemini~~ — RISOLTA (`03f`): `npm run retrieval:preview` su query EN vs IT equivalenti — stesso chunk top-1, similarity comparabile (0.68 vs 0.63); query "Idempotency" ritrova lo stesso chunk hybrid-only già noto da `02`. Nessuna degradazione, `gemini-embedding-001` regge il cross-linguale.
- ~~`architecture-context.md` non elencava `lib/rate-limit.ts`~~ — RISOLTA: §Project Structure aggiornata con `lib/rate-limit.ts`, `lib/security.ts`, `lib/chat/*`, `components/chat/*`.
- ~~`EMBEDDING_TIMEOUT_MS` dichiarata ma mai usata~~ — RISOLTA (`03f`), vedi sopra.
- ~~Fallimento Gemini a metà streaming silenzioso lato client~~ — CHIUSA come rischio residuo accettato (`03d`, step 8): limite del text-stream protocol (nessun canale errore), non risolvibile senza rompere la UI di `03b`. Mitigato lato client (`isEmptyAssistantResponse`), giudicato sufficiente per lo scope (single-user, no SLA).
- **Per `05a`** (ex `04a`, rinumerato dopo l'inserimento di `04 — Project Overview Panel`): security header a livello app (CSP, `X-Frame-Options`, ecc.) non coperti da `03d` (focalizzata sull'endpoint `/api/chat`) — da valutare esplicitamente prima dell'esposizione pubblica.
- ~~Per `03i`: come riconoscere l'errore di quota Gemini~~ — RISOLTA in fase di spec (non ancora implementata): verificato leggendo i sorgenti installati (`ai`, `@ai-sdk/google`, `@ai-sdk/provider`), non la doc web. `streamText()` non lancia mai in modo sincrono un errore di quota (emerge come part `"error"` dentro `result.fullStream`, non da un `throw`/reject di `streamChatResponse`) — un try/catch attorno alla sola chiamata non basta, serve un "peek" del primo elemento dello stream. Riconoscimento: `APICallError` (da `"ai"`) con `.statusCode === 429` o `.data.error.status === "RESOURCE_EXHAUSTED"`. Dettaglio completo in `feature-spec/03i-quota-error-handling.md` §Nota tecnica verificata.
- **Per `04`**: come implementare lo switch bilingue solo per il pannello overview senza reintrodurre `next-intl`/`LocaleSwitch` a livello di UI generale (componente locale al pannello con testo hardcoded IT/EN? Riuso mirato di `next-intl` solo lì? Da decidere in fase di spec).
- **Per il sottodominio**: nome esatto non ancora deciso (proposta `kb.davidesambughi.dev`, da confermare).

---

## Architecture Decisions

<!-- Decisioni tecniche rilevanti con motivazione compressa. Dettaglio completo di ogni indagine nelle spec citate, non ripetuto qui. -->

- **03e — Technical Knowledge Dashboard**: trasformazione da chat widget generico a dashboard per sviluppatori (trasparenza fonti, empty state orientato ai vincoli, rifiuto in tono neutro non-errore). Vedi §Completed.
- **Timeout esplicito in `streamChatResponse` (`generation.ts`)** — mancava (Invariant #13 non pienamente rispettato): una richiesta Gemini appesa restava tale indefinitamente. Fix: `GENERATION_TIMEOUT_MS=30_000` via parametro nativo `timeout` di `streamText`. Verificato abbassandolo a 1ms (fail in ~0.76s). Stesso limite noto: l'abort resta solo in log server-side, non arriva al client col text-stream protocol.
- **Commento impreciso in `onError` (`generation.ts`) riscritto** — diceva che l'errore a metà stream restava "responsabilità del chiamante/route", falso (gestito internamente, mai propagato). Riscritto per riflettere la realtà e rimandare all'open question chiusa sopra.
- **`onError` calcola `elapsedMs` come `onFinish`** — simmetria di logging tra ramo successo/errore.
- **Bug corretto: body non-JSON su `POST /api/chat` → 500 invece di 400** — `req.json()` non era in try/catch. Fix applicato, riverificato (400 su JSON malformato/troncato, streaming invariato su richiesta valida).
- **`toTextStreamResponse()` → `toTextStream`/`createTextStreamResponse`** — il metodo usato in `03a` risulta deprecato nella versione installata di `ai` (7.0.66), non segnalato dalla doc web scrapata in fase di spec. Stesso comportamento, nessun impatto su `03b`. Lezione: controllare anche i deprecation warning del pacchetto reale, non solo la doc web, su librerie in rapida evoluzione.
- **`gemini-2.5-flash` → `gemini-3.1-flash-lite`** — il primo, verificato attivo su doc pubblica in fase di spec, viene rifiutato a runtime con la chiave reale del progetto ("no longer available to new users" — probabile limite per-account non riflesso nella doc). Diagnosticato via `GET /v1beta/models` con la chiave reale. Lezione: per stato disponibile/deprecato per-account, una chiamata reale batte la doc pubblica anche se appena verificata.
- **Limite noto: fallimento Gemini a metà streaming invisibile al client con `toTextStreamResponse()`** — a differenza di `toUIMessageStreamResponse()` (canale errore SSE), il text-stream protocol non permette di iniettare un errore nello stream: client riceve 200 con body vuoto/troncato. Non risolto in `03a` (scelta valida per curl-testabilità), mitigato in `03b` (UI tratta risposta vuota come errore).
- **Bug corretto in `hybrid_search`: `LIMIT` senza `ORDER BY` esplicito nella CTE `text_results`** — l'`ORDER BY` dentro `row_number() OVER(...)` calcola i rank ma non vincola quali righe sopravvivono al `LIMIT` esterno (a differenza di `vector_results`, già corretta). Invisibile con corpus piccolo (match full-text sempre sotto il cap di 50). Fix: `order by ts_rank_cd(...) desc` esplicito prima del `limit`. Riverificato con query ad alto match count ("user", 67 risultati).
- **`similarity` in hybrid search non riflette l'ordine dei risultati (nota, non bug)** — l'ordine segue `fused.score` (RRF), `similarity` è solo un valore informativo ricalcolato separatamente; le due misure divergono per design quando il full-text contribuisce al ranking. Non viola lo schema. Documentato in `tech-spec.md` e come commento in `retrieval.ts`.
- **Fusione RRF lato SQL, non TypeScript** — riusa il pattern ufficiale Supabase, un solo round-trip di rete, nessuna logica duplicata client-side. Conseguenza: nessuna `fuseResults` in `retrieval.ts` (dichiarato in `02` §Scope Limits).
- **Nessun `match_threshold` in `02`** — non richiesto da nessun invariant, top-k fisso sufficiente per lo scope attuale. Riconsiderare solo se emergono risultati non pertinenti.
- **Identificatori con underscore in full-text search: query di frase (FOLLOWED BY), non "non riconosciuti"** — correzione di una nota precedente errata. `websearch_to_tsquery` tokenizza parole con underscore in `hword` composti (intero + componenti), producendo una query di adiacenza esatta (`<->`/`<N>`), non un AND libero — verificato su doc Postgres + empiricamente sul DB. Il match fallisce solo se la punteggiatura circostante altera la tokenizzazione. Nessun workaround, fuori scope `02`.
- **OpenAI → Gemini per embeddings e generazione, chiave unica `GEMINI_API_KEY`** — OpenAI privo di free tier reale (richiede billing dalla prima chiamata). Gemini via AI Studio ha free tier reale per entrambi. `gemini-embedding-001` supporta 1536 dim (Matryoshka) — nessuna migrazione schema Supabase necessaria. Non documentati come fatto accertato: i numeri esatti di rate limit Gemini (variano per progetto secondo Google) — `EMBEDDING_BATCH_SIZE=20` è una scelta conservativa.
- **Script standalone caricano `.env.local` via `tsx --env-file=.env.local`** — `tsx` non lo fa automaticamente come Next.js; `lib/env.ts` valida a import-time (Invariant #5).
- **`previewCorpus` (01a) resta raggiungibile via flag `--preview`, non sostituita da `runIngest`** — evita di rendere irraggiungibile l'ispezione a costo zero, contraddicendo l'intento originale dello script.
- **Testing non più escluso di default — Vitest, valutato caso per caso** — richiesta esplicita dell'utente dopo `01a`/`01b`. `architecture-context.md` §Testing Policy: mai test automatici contro OpenAI/Supabase/Gemini reali, solo logica pura estratta e testata. Applicato retroattivamente a `01a`; `00` esclusa consapevolmente (solo scaffold).
- **`lib/supabase/admin.ts` centralizza il client server-only** — fissato prima di `01b` per evitare duplicazione tra ingest e retrieval.
- **Ingest svuota `document_chunks` ad ogni run** (`delete().not("id","is",null)`, richiesto da supabase-js che vuole sempre un filtro) — garantisce idempotenza in sviluppo.
- ~~Embeddings: batch 100/OpenAI~~ — SUPERATA dal passaggio a Gemini (batch 20).
- ~~Pacchetto `openai`~~ — SUPERATA da `@google/genai`.
- **Chunking: nessuna soglia di lunghezza, fence-awareness generalizzata a tutti i blocchi fenced** (non solo Mermaid) — un commento `#` dentro un blocco bash/Python andrebbe altrimenti letto come heading. Confermato su corpus reale.
- **Corpus curato manualmente a 8 file piatti** — ridotto da 151+ file annidati per rispettare "corpus fisso, piccolo" (`project-overview.md` §Constraints).
- **Chiavi Supabase: `sb_publishable_...`/`sb_secret_...`, non più `anon`/`service_role`** — legacy key non più fornite ai nuovi progetti da nov 2025.
- **RLS abilitata su `document_chunks` senza policy** — tutto l'accesso è server-side via `SUPABASE_SECRET_KEY` (bypassa RLS); nessuna query client-side prevista, quindi nega di default via chiave pubblica (rinforza Invariant #20).
- **`03b`: conversione formato messaggi lato client, non riapertura di `03a`** — `useChat` invia `UIMessage[]` (`{role, parts}`), `ChatRequestSchema` si aspetta `{role, content}`. Scelto convertire via `toChatRequestMessages` invece di modificare uno schema già chiuso e verificato. Confermato anche che il text-stream protocol richiede `TextStreamChatTransport` esplicito e non porta canale errore (`status` resta `"ready"` anche su fallimento).
- **`next-intl` senza i18n routing (cookie-based)** — coerente con "lingua preferenza utente esplicita, non auto-detected", nessun bisogno di URL localizzati per SEO su tool a pagina singola.
- **Bug multi-turno corretto prima dell'implementazione di `03b`: `toChatRequestMessages` deve scartare messaggi con `content` vuoto** — senza filtro, una risposta assistant vuota/troncata (limite noto sopra) sarebbe rimandata al server come `content: ""` al turno successivo, bloccando l'intera conversazione con 400 (`ChatRequestSchema` richiede `min(1)`), non solo il turno fallito.
- **Micro-decisione post-`03b`: Markdown, font Inter, tema Blue**:
  - Markdown: `react-markdown` solo su messaggi assistant, deliberatamente **senza `rehype-raw`** (HTML grezzo resta escapato — il testo viene da un LLM). Nessun `@tailwindcss/typography`, solo selettori Tailwind arbitrari per spaziatura base.
  - Font: Geist → Inter (Google Sans scartato, proprietario). Bug preesistente scoperto e corretto nello stesso passaggio: variabile nominata `--font-geist-sans` mentre `globals.css` si aspettava `--font-sans` — `font-sans` non applicava mai Geist fin dallo scaffold.
  - Colore: preset ufficiale Blue di shadcn (`shadcn apply --preset b1PzeK --only theme`), solo token OKLCH generati ufficialmente (Invariant #10).
    Verificato in browser reale, 33/33 test.
- **`03b`: costanti locale spostate in `lib/i18n/locale.ts`** — bug reale da `npm run build`: `locale-switch.tsx` (client) importava da `i18n/request.ts` (server-only, `next/headers`), Turbopack falliva. Fix: costanti pure in file senza dipendenze server-only.
- **`03b`: scrittura `document.cookie` estratta in funzione a livello di modulo** — `eslint-plugin-react-hooks` (React Compiler) trattava la mutazione diretta nel componente come impura.
- **`architecture-context.md` §Stack già corretto (Gemini)** — una revisione esterna segnalava un riferimento OpenAI stale lì, ma era già allineato; il riferimento stale reale era in `project-overview.md` §Aggiornamento scope (claim sul retrieval cross-linguale mai aggiornato), corretto lì.

---

## Session Notes

- **Regola di processo attiva**: quando si tocca un file appartenente a una unit già chiusa, dichiarare esplicitamente nel riepilogo cosa si trova in quel file al di fuori delle proprie modifiche (fix/deviazioni pregresse incluse), non solo cosa si è cambiato — anche se il codice esistente è corretto. (Nata da un caso reale in `03c`: il fix `toTextStreamResponse` di `03a`, osservato ma non dichiarato durante l'edit di `03c`, scoperto dall'utente solo in review file-by-file.)
- **Schema riusabile per revisioni esterne (altro LLM) su spec/codice**: verificare sempre con ricerca propria prima di applicare una correzione proposta, non fidarsi né rifiutare per default — track record storico ~50/50. Applicato con successo su `03a` (3 correzioni, tutte verificate) e `03d` (3 correzioni post-riepilogo, tutte verificate indipendentemente).
- Ultimo step completato (2026-08-16): `03d-security-review.md` implementata e verificata end-to-end. `03f` chiude le ultime open question non bloccanti pre-`04a`.
- Feature-list rinumerata (2026-08-23): inserito `04 — Project Overview Panel` (recruiter-facing, idea grezza da rifinire) prima del deploy — `04 — Deploy & Portfolio Wiring` diventa `05`, `05 — Learning Synthesis` diventa `06`.
- Prossimo step: `05a — Deploy su Vercel` (ex `04a`), con security header a livello app (vedi §Open Questions) da valutare esplicitamente prima dell'esposizione pubblica. `04 — Project Overview Panel` va rifinito in una feature-spec dedicata prima di essere implementato — valutare se farlo prima o in parallelo a `05a`.
